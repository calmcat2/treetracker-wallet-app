# This workflow triggers on pull requests that modify files in the web module (i.e., apps/web/**)
# It runs component and end-to-end (E2E) tests to validate changes.

name: PR for WEB module

on:
  pull_request:
    branches:
      - "*"
    paths:
      - "apps/web/**"
      - ".github/workflows/ci-cd-pr-web-wallet.yml"
      - "!**/*.md"
  workflow_dispatch:

jobs:
  component-e2e-tests:
    runs-on: ubuntu-latest
    env:
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
      NEXT_PUBLIC_WALLET_APP_API: ${{ secrets.NEXT_PUBLIC_WALLET_APP_API }}
      NEXT_PUBLIC_ENV: ${{ secrets.NEXT_PUBLIC_ENV }}
      NEXT_PUBLIC_API_TIMEOUT: ${{ secrets.NEXT_PUBLIC_API_TIMEOUT }}
      NEXT_PUBLIC_API_RETRIES: ${{ secrets.NEXT_PUBLIC_API_RETRIES }}
      NEXT_PUBLIC_BASE_URL: ${{ secrets.NEXT_PUBLIC_BASE_URL }}
      PRIVATE_KEYCLOAK_REALM: ${{ secrets.PRIVATE_KEYCLOAK_REALM }}
      PRIVATE_KEYCLOAK_BASE_URL: ${{ secrets.PRIVATE_KEYCLOAK_BASE_URL }}
      PRIVATE_KEYCLOAK_CLIENT_SECRET: ${{ secrets.PRIVATE_KEYCLOAK_CLIENT_SECRET }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      PRIVATE_KEYCLOAK_CLIENT_ID: ${{ secrets.PRIVATE_KEYCLOAK_CLIENT_ID }}
      PRIVATE_WALLET_API_KEY: ${{ secrets.PRIVATE_WALLET_API_KEY }}
      CORS_ORIGINS_DEV: ${{ secrets.CORS_ORIGINS_DEV }}
      CORS_ORIGINS: ${{ secrets.CORS_ORIGINS }}

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Start user service in the background
        run: |
          yarn user:dev &
          sleep 5

      - name: Run component test
        uses: cypress-io/github-action@v6
        with:
          command: yarn cypress-component-test

      - name: Run Cypress E2E Tests
        uses: cypress-io/github-action@v6
        with:
          command: yarn cypress-e2e-headless-test
